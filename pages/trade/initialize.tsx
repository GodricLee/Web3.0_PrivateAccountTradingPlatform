import { Default } from 'components/layouts/Default';
import {TradeInitialize} from 'components/templates/Trade/';

const Trade_Initialize = () => {
  return (
    <Default pageName="Trade Initialize">
      <TradeInitialize />
    </Default>
  );
};

export default Trade_Initialize;
